import { describe, it, expect } from 'vitest'

// Placeholder — remove once real component tests are added
describe('setup', () => {
  it('vitest is configured correctly', () => {
    expect(true).toBe(true)
  })
})
